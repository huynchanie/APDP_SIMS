<nav class="navbar navbar-expand-lg" style="background-color: #F07025; color: #fff; padding: 10px 20px;">
    <div class="container-fluid d-flex justify-content-between align-items-center">
        <!-- Logo -->
        <a class="navbar-brand fw-bold" href="@Url.Action("Index", "Dashboard")" style="color: #fff;">
            🎓 Teacher Student
        </a>

        <!-- Menu -->
        <div class="d-flex gap-4">
            <a class="nav-link" href="@Url.Action("Index", "Admin")" style="color: #fff;">Dashboard</a>
           
            <a class="nav-link" href="@Url.Action("DashboardCourse", "Course")" style="color: #fff;">Xem khóa học</a>
            <a class="nav-link" href="@Url.Action("AssignGrade", "Grade")" style="color: #fff;">Quản lý điểm</a>
        </div>

        <!-- Đăng xuất -->
        <div>
            <a class="nav-link logout-link" href="@Url.Action("Index", "Home")" style="color: #fff;">Đăng xuất</a>
        </div>
    </div>
</nav>
